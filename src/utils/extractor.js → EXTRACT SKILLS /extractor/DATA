export const extractData = (text) => {
  const skills = ["javascript", "node", "react", "python"];

  const foundSkills = skills.filter(skill =>
    text.toLowerCase().includes(skill)
  );

  return {
    skills: foundSkills,
    length: text.length
  };
};
